// @flow

import {
    SET_LIST_DATA,
    CLEAR_LIST_DATA,
  } from '../actions/LevelOne';
  
  export default (state = {
    aa: false,
  }, action) => {
    switch (action.type) {
      default:
        return state;
    }
  };
  